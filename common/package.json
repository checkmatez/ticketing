{
  "name": "@chm-tickets/common",
  "version": "1.0.0",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "del ./build/*"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^3.9.5"
  }
}
